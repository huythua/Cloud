# .env.example - Mẫu cấu hình biến môi trường cho backend Cloud DB MVP
# Đổi tên file này thành .env và chỉnh sửa giá trị phù hợp trước khi chạy thật

# Kết nối MySQL để provision database vật lý
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_ADMIN_USER=root
MYSQL_ADMIN_PASSWORD=your_mysql_root_password

# Secret key cho JWT
SECRET_KEY=your_secret_key_here

# Kết nối metadata DB (nếu không dùng SQLite mặc định)
# METADATA_DATABASE_URL=sqlite:///./metadata.db
